# H179 independent cross-system action/order challenge

Date: 2026-07-27

Status: `pass_with_scope`

## Result

An independent Node.js implementation reproduced the H178 fixed-roster
decision from the source-coding record without importing the Python producer.
It verified all nine source hashes, the H177 roster binding, seven-system
coverage, and all 70 system-by-unit rows.

The reconstruction agrees that:

- no non-RoboArena system in the fixed frame supplies a second affirmative
  instance of the RoboArena-like global-action/context-order mismatch;
- UMI-Bench and RoboDojo each satisfy the prespecified positive-contrast
  conjunction; and
- both positive contrasts are `source_described`, not independently
  artifact-reproduced.

All 16 leave-one-required-unit-out attacks and six shortcut attacks were
rejected. A fixed task list, policy queue, reset prose, episode totals,
leaderboard label, or sim/real correlation statement cannot alone promote a
system.

## Paper consequence

H178--H179 pass H176's empirical promotion gate through two materially
different positive protocol contrasts. This changes P2 from
`paper_candidate_not_manuscript_ready` to
`paper_candidate_empirical_contrast_gate_passed`.

The change does not authorize broad novelty claims or outcome interpretation.
Before claim drafting, the combined robotics identification-to-operation unit
still needs an adverse synthesis-level novelty challenge. The UMI-Bench and
RoboDojo execution packages also remain weaker than independently reproduced
artifacts, which must be disclosed if the paper relies on them.

## Scope

The seven-system roster was purposive and fixed before full-text screening.
It establishes contrasting mechanisms, not field prevalence. No performance
direction, magnitude, significance, or leaderboard standing was extracted or
used. No roster expansion, external contact, site access, or P3 access
occurred.

Canonical artifacts:

- `input-h178-source-coding.json`;
- `result-h178-cross-system-action-order-source-audit.json`;
- `challenge_h179_cross_system_action_order.mjs`; and
- `result-h179-cross-system-action-order-independent-challenge.json`.
